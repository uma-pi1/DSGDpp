* Lock management for values stored in environments
* Relocation of variables to new nodes (with redirections)
* Persistent variables (stored on disk, can be cached in memory)
* Replicated variables (stored on one or more nodes)
* Support for function calls
* Implement MapReduce framework
* Add all of MPI's communication methods to Channel
* Replace IdentifiableTask template by a superclass
* Error handling?
* Fault tolerance?
* More flexible methods to spawn tasks (e.g., precisely k tasks)
* Support for createOrSet in environment